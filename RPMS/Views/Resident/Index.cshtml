@model IEnumerable<Resident>

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<div class="mt-3 mx-3">
    <h3>Brgy. Niing</h3>
   @*  <div class="table-responsive">
        <table class="table">
            <thead class="thead-light">
                <tr>

                    <th scope="col">First</th>
                    <th scope="col">Last</th>
                    <th scope="col">Age</th>
                </tr>
            </thead>
            <tbody>

                @foreach (var item in Model.Select((value, i) => new { i, value }))
                {
                    <tr>

                        <td>@item.value.Firstname</td>
                        <td>@item.value.Lastname</td>
                        <td>@item.value.Age</td>

                    </tr>
                }


            </tbody>
        </table>
    </div> *@
    <table class="table align-middle mb-0 bg-white">
        <thead class="bg-light">
            <tr>
                <th>Name</th>
                <th>Street</th>
                @* <th>Status</th>
                <th>Position</th> *@
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Select((value, i) => new { i, value }))
            {
                <tr>
                    <td>
                        <div class="d-flex align-items-center">
                            <img src="https://mdbootstrap.com/img/new/avatars/8.jpg"
                                 alt=""
                                 style="width: 45px; height: 45px"
                                 class="rounded-circle" />
                            <div class="ms-3">
                                <p class="fw-bold mb-1">@item.value.Lastname, @item.value.Firstname</p>
                                <p class="text-muted mb-0">@item.value.Age</p>
                            </div>
                        </div>
                    </td>
                    <td>
                        <p class="fw-normal mb-1">@item.value.Address.Street</p>                        
                    </td>                    
                    <td>
                        <button type="button" class="btn btn-link btn-sm btn-rounded">
                            Edit
                        </button>
                    </td>
                </tr>
            }
            
        </tbody>
    </table>
</div>
